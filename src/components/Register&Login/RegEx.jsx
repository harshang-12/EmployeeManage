export const ValidName = new RegExp(
    '[a-zA-Z]'
);
export const ValidEmail = new RegExp(
    '[^\s@]+@[^\s@]+\.[^\s@]'
);
export const ValidPass = new RegExp(
    '[a-zA-z0-9]{8}'
);



// export const ValidPin = new RegExp(
//     '^[1-9]{1}[0-9]{5}$'
// )